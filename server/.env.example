# --- SERVER CONFIG ---
PORT=3000
NODE_ENV=development
# Client URL để cấu hình CORS (Web Dashboard)
CLIENT_URL=http://localhost:5173

# --- DATABASE (MONGODB ATLAS) ---
# Copy chuỗi kết nối từ Atlas, nhớ thay <password> bằng mật khẩu thật
MONGO_URI=mongodb+srv://<username>:<password>@cluster0.xxxxx.mongodb.net/collector_vault?retryWrites=true&w=majority

# --- SECURITY ---
# Gõ linh tinh một chuỗi dài để làm khóa mã hóa Token
JWT_SECRET=your_super_secret_key_here
JWT_EXPIRE=30d

# --- QUEUE (REDIS UPSTASH) ---
# Copy từ Upstash Dashboard (dạng redis://default:xxxx@....upstash.io:6379)
REDIS_URL=redis://default:your_password@your-instance.upstash.io:6379

# --- FILE STORAGE (CLOUDINARY) ---
# Copy từ Cloudinary Dashboard
CLOUDINARY_CLOUD_NAME=your_cloud_name
CLOUDINARY_API_KEY=your_api_key
CLOUDINARY_API_SECRET=your_api_secret

# --- AI SERVICE ---
# Link tới Python Worker (Sẽ điền sau khi deploy lên Hugging Face)
# Lúc dev local thì dùng localhost
AI_SERVICE_URL=http://localhost:8000
